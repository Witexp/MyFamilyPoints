import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    center: {
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
    },
    button: {
        
    },
    h1: {
        fontSize: 35,
        fontWeight: 'bold',
        marginBottom: 15,
    },
    h2: {
        fontSize: 28,
        marginBottom: 10,
    },
    text: {
        fontSize: 18,
    }
})
